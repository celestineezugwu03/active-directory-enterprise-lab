## Network Architecture

Windows Server (Domain Controller + DNS)
        |
        |
Windows 10 Client (Domain-joined)


The Domain Controller provides centralized authentication, directory services, DNS resolution, and access control enforcement across the simulated enterprise environment.
